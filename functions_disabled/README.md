# Archived Functions

This directory is archive-only.

Rules:
- Files under `functions/` are the only authoritative deployable functions.
- Matching names under `functions_disabled/` are retained only for historical reference.
- Do not deploy from this directory.
- Any intentional archived duplicate must be listed in `.archive_manifest.txt`.
