table:
  name: v_chat_user_lookup
  schema: public
select_permissions:
  - role: employee
    permission:
      columns:
        - user_uid
        - email
        - account_id
        - role
        - chat_code
      filter: {}
  - role: me
    permission:
      columns:
        - user_uid
        - email
        - account_id
        - role
        - chat_code
      filter: {}
  - role: owner
    permission:
      columns:
        - user_uid
        - email
        - account_id
        - role
        - chat_code
      filter: {}
  - role: superadmin
    permission:
      columns:
        - user_uid
        - email
        - account_id
        - role
        - chat_code
      filter: {}
  - role: user
    permission:
      columns:
        - user_uid
        - email
        - account_id
        - role
        - chat_code
      filter: {}
